baseURL = "https://falkensmage.com/"
languageCode = "en-us"
title = "Falken's Mage"
theme = "arcaeon"

[params]
  author = "Matt Stine"
  description = "Falken's Mage - Matt Stine"
  plausible = "falkensmage.com"

# Single source of truth for Cal.com booking link-outs (OFFER-05).
# Live event-type URLs (OFFER-F1 closed 2026-07-04) — swap the three
# values here, nowhere else. Templates dereference site.Params.booking
# by each offer page's booking_key; never hardcode a Cal.com URL in a
# template.
[params.booking]
  "the-query" = "https://cal.com/falkensmage/the-query"
  "the-cast" = "https://cal.com/falkensmage/the-cast"
  "the-daemon" = "https://cal.com/falkensmage/clarity-consult"

[params.colors]
  electricViolet = "#7a2cff"
  neonMagenta = "#ff3cac"
  plasmaPink = "#ff5fd2"
  electricBlue = "#2fd3ff"
  solarWhite = "#fff4e6"
  fusionGold = "#ffb347"
  ignitionOrange = "#ff7a18"
  deepIndigo = "#0a0f3c"
  midnightBlue = "#111a6b"
  voidPurple = "#1a0f2e"
  ionGlow = "#5be7ff"

[caches]
  [caches.getresource]
    dir = ":cacheDir/:project"
    maxAge = "1h"
