---
phase: 02-static-content
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - tests/validate-phase-02.sh
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - IDENT-01
  - IDENT-02
  - SOCIAL-01
  - SOCIAL-02
  - SOCIAL-03
  - SOCIAL-04
  - SOCIAL-05
  - CTA-01
  - CTA-02
  - FOOT-01
  - FOOT-02
  - FOOT-03
must_haves:
  truths:
    - "Validation script checks all 16 Phase 2 requirements against built HTML"
    - "Hugo build produces zero errors"
    - "Visual checkpoint confirms mobile rendering at 375px"
  artifacts:
    - path: "tests/validate-phase-02.sh"
      provides: "Automated validation for all Phase 2 requirements"
      contains: "HERO-01"
  key_links:
    - from: "tests/validate-phase-02.sh"
      to: "public/index.html"
      via: "grep checks against Hugo build output"
      pattern: "grep.*public/index.html"
---

<objective>
Create the Phase 2 validation script that checks all 16 requirements against the built HTML, then pause for visual checkpoint at 375px mobile viewport.

Purpose: The validation script is the automated gate for `/gsd-verify-work`. The visual checkpoint catches rendering issues (image distortion, glow clipping, touch target sizing) that grep cannot detect.

Output: `tests/validate-phase-02.sh` + human visual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content/02-CONTEXT.md
@.planning/phases/02-static-content/02-VALIDATION.md
@.planning/phases/02-static-content/02-01-SUMMARY.md
@.planning/phases/02-static-content/02-02-SUMMARY.md

<interfaces>
<!-- From Phase 1 validation pattern -->
Phase 1 established: `tests/validate-phase-01.sh` — shell script that runs `hugo --minify`, then greps `public/index.html` and CSS files for required patterns. Each check prints PASS/FAIL with the requirement ID. Exits non-zero if any check fails.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 2 validation script</name>
  <files>tests/validate-phase-02.sh</files>
  <read_first>
    - tests/validate-phase-01.sh
    - public/index.html
    - themes/arcaeon/assets/css/main.css
    - .planning/phases/02-static-content/02-VALIDATION.md
  </read_first>
  <action>
Create `tests/validate-phase-02.sh` following the exact pattern established in `tests/validate-phase-01.sh`. The script must:

1. Run `hugo --minify` and capture exit code. If Hugo build fails, print FAIL and exit 1.

2. Check each of the 16 Phase 2 requirements against the built output. Each check is a grep against `public/index.html` or `themes/arcaeon/assets/css/main.css`. Print `PASS: [REQ-ID] description` or `FAIL: [REQ-ID] description` for each.

3. Track total PASS/FAIL count. Exit 0 if all pass, exit 1 if any fail.

**Required checks (16 total):**

| Check | Requirement | Grep Target | Pattern |
|-------|-------------|-------------|---------|
| 1 | HERO-01 | `public/index.html` | `picture` element present |
| 2 | HERO-02 | `public/index.html` | `display-text` class on page |
| 3 | HERO-03 | `public/index.html` | `hero-tagline` class on page |
| 4 | HERO-04 | `public/index.html` | `glow-ambient` class on page |
| 5 | IDENT-01 | `public/index.html` | `identity-statement` class on page |
| 6 | IDENT-02 | `content/_index.md` | `identity_statement:` key in front matter |
| 7 | SOCIAL-01 | `public/index.html` | `social-grid` class on page |
| 8 | SOCIAL-02 | `public/index.html` | `glow-interactive` class on page |
| 9 | SOCIAL-03 | `themes/arcaeon/assets/css/main.css` | `min-height: 44px` (or minified variant `min-height:44px`) in `.social-card` rule |
| 10 | SOCIAL-04 | `public/index.html` | All 8 platform domains present: `feralarchitecture.substack.com`, `linkedin.com`, `x.com/falkensmage`, `bsky.app`, `instagram.com/falkensmage`, `threads.com`, `open.spotify.com`, `my.tarotpulse.com` |
| 11 | SOCIAL-05 | `public/index.html` | `aria-label=` present (count >= 8) |
| 12 | CTA-01 | `public/index.html` | Text "Coaching" present on page (from "Coaching. Speaking. Collaboration.") |
| 13 | CTA-02 | `public/index.html` | `mailto:falkensmage@falkenslabyrinth.com` present AND `glow-radiant-core` class present |
| 14 | FOOT-01 | `public/index.html` | "Stay feral, folks." text present |
| 15 | FOOT-02 | `public/index.html` | "Digital Intuition LLC" text present |
| 16 | FOOT-03 | `public/index.html` | `footer-lemniscate` class present |

**Additional check (regression):**
| 17 | HERO-04 prefers-reduced-motion | CSS file | `prefers-reduced-motion` present (inherited from Phase 1, must not be removed) |

Make the script executable (`chmod +x`). Match the output format of `validate-phase-01.sh` exactly for consistency.
  </action>
  <verify>
    <automated>cd /Users/falkensmage/RitualSync/falkensmage-website && bash tests/validate-phase-02.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/validate-phase-02.sh` exists and is executable
    - Script contains checks for all 16 requirement IDs: HERO-01 through HERO-04, IDENT-01, IDENT-02, SOCIAL-01 through SOCIAL-05, CTA-01, CTA-02, FOOT-01 through FOOT-03
    - Script runs `hugo --minify` as first step
    - Script exits 0 when all checks pass
    - Script exits 1 when any check fails
    - Script prints PASS/FAIL for each individual check
    - `bash tests/validate-phase-02.sh` produces all PASS lines when run after Plans 01 and 02 are complete
  </acceptance_criteria>
  <done>Validation script exists, is executable, checks all 16 Phase 2 requirements, and reports PASS for each. Both `validate-phase-02.sh` and `validate-phase-01.sh` exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual checkpoint — full page at 375px and desktop</name>
  <files>none</files>
  <action>
Human verifies the complete Phase 2 visual rendering. All four sections built by Plans 01 and 02 must render correctly at mobile and desktop viewports.

What was built: Complete Phase 2 — all four visible sections of falkensmage.com. Hero (Magus image with ambient glow pulse + "Falken's Mage" logotype + tagline), Identity+CTA (identity statement + "Work With Me" mailto button with Radiant Core glow), Social Links (8 platforms in ghost card grid with hover glow), Footer ("Stay feral, folks." + lemniscate + copyright).

How to verify:
1. Start dev server: `hugo server --buildDrafts`
2. Open browser to `http://localhost:1313`
3. Set DevTools to 375px wide viewport (mobile)
4. Verify top-to-bottom:
   a. Magus image renders full-width, center-cropped, no stretching
   b. Electric Violet ambient glow pulse visible behind image (subtle purple bleed)
   c. "Falken's Mage" in large Cinzel display weight below image
   d. Tagline text below logotype
   e. Section background shifts from Void Purple (hero) to Deep Indigo (identity+CTA)
   f. Identity statement text is readable and present
   g. "Coaching. Speaking. Collaboration." text visible
   h. "Work With Me" button has warm gold-to-orange gradient
   i. Background shifts back to Void Purple (social links)
   j. 8 social links in 2-column grid, ghost card borders visible
   k. Hover a social card — Electric Violet glow appears, label turns Ion Glow blue
   l. Each card has an icon + label
   m. Footer shows "Stay feral, folks." in Cinzel, lemniscate symbol, copyright year
5. Switch to desktop width (1200px+):
   a. Image scales wider, social grid becomes 4 columns
   b. Content max-width is bounded (not full-width text)
6. Click "Work With Me" button — should open email client (mailto link)
7. Click any social link — should open in new tab
8. In DevTools, enable "prefers-reduced-motion: reduce" — ambient glow pulse should stop
  </action>
  <verify>Human visual confirmation at 375px and desktop viewports</verify>
  <done>Human types "approved" confirming all sections render correctly, or describes specific issues to fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Validation script reads local files only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Validation script | accept | Script is local, runs in dev environment. No user input to script. Not a production artifact. |
</threat_model>

<verification>
`bash tests/validate-phase-02.sh` exits 0 with all 16+ checks passing. `bash tests/validate-phase-01.sh` exits 0 (regression). Human confirms visual rendering at 375px and desktop widths.
</verification>

<success_criteria>
- Validation script covers all 16 Phase 2 requirements
- Both Phase 1 and Phase 2 validation scripts pass
- Human confirms: Magus image renders correctly, glow pulse visible, all sections present and styled, social cards interactive, mailto CTA works, reduced-motion respected
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content/02-03-SUMMARY.md`
</output>
