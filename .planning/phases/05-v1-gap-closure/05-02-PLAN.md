---
phase: 05-v1-gap-closure
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/validate-phase-03.sh
  - .github/workflows/hugo.yml
  - .planning/phases/01-theme-foundation/01-02-SUMMARY.md
  - .planning/phases/01-theme-foundation/01-03-SUMMARY.md
  - .planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md
  - .planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "tests/validate-phase-03.sh line 154 accepts canonical URL with or without trailing slash"
    - "tests/validate-phase-03.sh has new Phase 5 checks for A11Y-01, PERF-03 URL parity, no-woff2-in-css, npm-ci-absent, and frontmatter-backfill-present"
    - "bash tests/validate-phase-03.sh exits 0 after Plan 01's fixes are applied"
    - ".github/workflows/hugo.yml contains zero occurrences of 'npm ci'"
    - "01-02-SUMMARY.md frontmatter contains requirements-completed: [THEME-02, THEME-03]"
    - "01-03-SUMMARY.md frontmatter contains requirements-completed: [THEME-04, THEME-05, THEME-06]"
    - "03-01-SUMMARY.md frontmatter contains requirements-completed: [CURR-01, CURR-02, CURR-03, PERF-03, A11Y-01, A11Y-04]"
    - "03-02-SUMMARY.md frontmatter contains requirements-completed: [SEO-01, SEO-02, SEO-03, PERF-01, PERF-02, PERF-04, A11Y-02, A11Y-03]"
  artifacts:
    - path: "tests/validate-phase-03.sh"
      provides: "trailing-slash-tolerant SEO-03 canonical check plus 5 Phase-5 structural checks"
      contains: "Phase 5"
    - path: ".github/workflows/hugo.yml"
      provides: "CI workflow without vestigial npm ci step"
      contains: "Install Hugo CLI"
    - path: ".planning/phases/01-theme-foundation/01-02-SUMMARY.md"
      provides: "Traceability: THEME-02, THEME-03 marked complete"
      contains: "requirements-completed"
    - path: ".planning/phases/01-theme-foundation/01-03-SUMMARY.md"
      provides: "Traceability: THEME-04, THEME-05, THEME-06 marked complete"
      contains: "requirements-completed"
    - path: ".planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md"
      provides: "Traceability: 6 REQ-IDs marked complete"
      contains: "requirements-completed"
    - path: ".planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md"
      provides: "Traceability: 8 REQ-IDs marked complete"
      contains: "requirements-completed"
  key_links:
    - from: "tests/validate-phase-03.sh"
      to: "public/index.html, public/css/main.min.*.css, .github/workflows/hugo.yml, SUMMARY.md files"
      via: "grep assertions run after hugo build"
      pattern: "Phase 5"
---

<objective>
Close the remaining v1.0 audit items: the SEO-03 regex false-failure in `validate-phase-03.sh`, the vestigial `npm ci` step in the CI workflow, and the missing `requirements-completed` frontmatter across 4 SUMMARY.md files spanning Phase 1 and Phase 3. Also add five new structural checks to `validate-phase-03.sh` so Plan 01's fixes are continuously verified — A11Y-01 footer count, PERF-03 preload/@font-face URL parity (both fonts), no `.woff2` in `public/css/`, `npm ci` absent from workflow YAML, and `requirements-completed` present in all four SUMMARY files.

This plan depends on Plan 01 (Wave 1) because the new structural checks in `validate-phase-03.sh` only pass if Plan 01's code fixes are already applied. Six sibling tasks, editing six disjoint files, fully parallel-eligible within the wave.

Purpose: Restore milestone audit traceability (every v1.0 REQ-ID appears in at least one SUMMARY), eliminate the false 32/33 failure, and harden the validation script against future regression of Plan 01's fixes. PERF-01 requirement ID is carried by this plan because Hugo-level performance hygiene (clean CI, accurate validation, no false failures) is prerequisite to any sub-1s page-load claim.
Output: Patched validation script with 5 new checks, workflow without `npm ci`, four SUMMARY files with backfilled `requirements-completed` frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/05-v1-gap-closure/05-RESEARCH.md
@.planning/phases/05-v1-gap-closure/05-VALIDATION.md
@.planning/ROADMAP.md
@.planning/phases/01-theme-foundation/01-02-PLAN.md
@.planning/phases/01-theme-foundation/01-03-PLAN.md
@.planning/phases/03-dynamic-layer-quality/03-01-PLAN.md
@.planning/phases/03-dynamic-layer-quality/03-02-PLAN.md
@tests/validate-phase-03.sh
@.github/workflows/hugo.yml

<interfaces>
Current `tests/validate-phase-03.sh` line 154 (the target line to patch) — see the file directly; the line reads:
`check 'canonical points to falkensmage.com' "$(grep -c 'href=(HTTPS_QUOTED_URL)' "$INDEX")"` where HTTPS_QUOTED_URL is the literal-no-trailing-slash form. Replacement keeps the `check` label verbatim and switches the grep to `-cE` with the trailing-slash-optional pattern (see Task 1 action).

Current `.github/workflows/hugo.yml` lines 48–49 are the `Install Node.js dependencies` step + its `run:` body. Both lines delete cleanly; `Setup Pages` precedes them, `Build with Hugo` follows.

REQ-ID lists for frontmatter backfill (copied verbatim from each PLAN's `requirements:` field):
- 01-02-PLAN.md `requirements:` -> `[THEME-02, THEME-03]`
- 01-03-PLAN.md `requirements:` -> `[THEME-04, THEME-05, THEME-06]`
- 03-01-PLAN.md `requirements:` -> `[CURR-01, CURR-02, CURR-03, PERF-03, A11Y-01, A11Y-04]`
- 03-02-PLAN.md `requirements:` -> `[SEO-01, SEO-02, SEO-03, PERF-01, PERF-02, PERF-04, A11Y-02, A11Y-03]`

Canonical frontmatter key name (verified against audit SDK source `commands.cjs:463`): `requirements-completed` — hyphen, not underscore. Use inline array YAML only (matches existing 01-01, 02-01, 02-02, 04-01, 04-02 pattern).

Existing `check` helper in validate-phase-03.sh (lines 22–32): takes a label string and an integer; if the integer is > 0, increments PASS, else increments FAIL. The script uses `set -uo pipefail` on line 7, so any grep/command in a pipeline that exits non-zero on zero matches aborts the script unless appended with `|| true` or captured defensively.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch validate-phase-03.sh — SEO-03 regex plus 5 new Phase-5 structural checks</name>
  <files>tests/validate-phase-03.sh</files>
  <read_first>
    - tests/validate-phase-03.sh (entire file — the `check` helper is on line 22; SEO-03 on line 154; set -uo pipefail on line 7)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Code Examples Example 3 (exact regex replacement)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Validation Architecture (new-check command specs)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Common Pitfalls Pitfall 3 and Pitfall 7 (grep portability, `<footer[ >]` pattern)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Landmines Landmine 7 (pipefail + `|| true` convention)
  </read_first>
  <action>
Make two kinds of edits to `tests/validate-phase-03.sh`:

**A) Patch line 154 — SEO-03 regex fix.**

BEFORE the edit, line 154 reads exactly:
    `check 'canonical points to falkensmage.com' "$(grep -c 'href="https://falkensmage.com"' "$INDEX")"`

AFTER the edit, line 154 must read exactly:
    `check 'canonical points to falkensmage.com' "$(grep -cE 'href="https://falkensmage\.com/?"' "$INDEX")"`

Changes: `-c` becomes `-cE` (extended regex); the bare literal URL becomes the ERE pattern `href="https://falkensmage\.com/?"` with an escaped dot and an optional trailing slash. This accepts BOTH `href="https://falkensmage.com"` and `href="https://falkensmage.com/"` (Hugo's actual output includes the trailing slash per hugo.toml baseURL).

Preserve lines 152, 153, 155 unchanged. Do not touch the `check 'canonical URL present'` line above it.

**B) Append 5 new structural check blocks.**

Insert the block below AFTER the existing `echo "--- SEO-03: Canonical URL ---"` section (it ends just after line 155) and BEFORE the final `echo "=== Results: ..."` summary (currently line 157). The new blocks use `|| true` on every grep/ls that may legitimately match zero items (per Landmine 7 — `set -uo pipefail` aborts otherwise).

Block to insert (verbatim):

```
# --- Phase 5 gap closure: A11Y-01 nested-footer guard ---
echo "--- A11Y-01 (Phase 5): Exactly one footer landmark ---"
FOOTER_OPEN=$(grep -cE '<footer[ >]' "$INDEX" || true)
if [ "$FOOTER_OPEN" -eq 1 ]; then
  echo "  PASS: exactly one <footer> opening tag in built HTML"
  PASS=$((PASS + 1))
else
  echo "  FAIL: expected 1 <footer> opening tag, found $FOOTER_OPEN"
  FAIL=$((FAIL + 1))
fi
echo ""

# --- Phase 5 gap closure: PERF-03 preload href === @font-face url for both fonts ---
echo "--- PERF-03 (Phase 5): Preload href matches @font-face url ---"
CSS_BUILD=$(ls "$BUILD_DIR"/css/main.min.*.css 2>/dev/null | head -1)
if [ -z "$CSS_BUILD" ]; then
  echo "  FAIL: no built CSS found in public/css/"
  FAIL=$((FAIL + 1))
else
  CINZEL_PRELOAD=$(grep -oE '/fonts/cinzel-[^"]*\.woff2' "$INDEX" | head -1 || true)
  CINZEL_URL=$(grep -oE '/fonts/cinzel-[^)]*\.woff2' "$CSS_BUILD" | head -1 || true)
  if [ -n "$CINZEL_PRELOAD" ] && [ "$CINZEL_PRELOAD" = "$CINZEL_URL" ]; then
    echo "  PASS: Cinzel preload href ($CINZEL_PRELOAD) matches @font-face url"
    PASS=$((PASS + 1))
  else
    echo "  FAIL: Cinzel preload='$CINZEL_PRELOAD' vs url='$CINZEL_URL'"
    FAIL=$((FAIL + 1))
  fi
  SG_PRELOAD=$(grep -oE '/fonts/space-grotesk-[^"]*\.woff2' "$INDEX" | head -1 || true)
  SG_URL=$(grep -oE '/fonts/space-grotesk-[^)]*\.woff2' "$CSS_BUILD" | head -1 || true)
  if [ -n "$SG_PRELOAD" ] && [ "$SG_PRELOAD" = "$SG_URL" ]; then
    echo "  PASS: Space Grotesk preload href ($SG_PRELOAD) matches @font-face url"
    PASS=$((PASS + 1))
  else
    echo "  FAIL: Space Grotesk preload='$SG_PRELOAD' vs url='$SG_URL'"
    FAIL=$((FAIL + 1))
  fi
fi
echo ""

# --- Phase 5 gap closure: No .woff2 duplicated into public/css/ ---
echo "--- PERF-03 (Phase 5): Fonts not duplicated into public/css/ ---"
CSS_WOFF2_COUNT=$(ls "$BUILD_DIR"/css/*.woff2 2>/dev/null | wc -l | tr -d ' ')
if [ "$CSS_WOFF2_COUNT" -eq 0 ]; then
  echo "  PASS: public/css/ contains zero .woff2 files"
  PASS=$((PASS + 1))
else
  echo "  FAIL: public/css/ contains $CSS_WOFF2_COUNT .woff2 file(s) -- esbuild copied fonts"
  FAIL=$((FAIL + 1))
fi
echo ""

# --- Phase 5 gap closure: npm ci absent from CI workflow ---
echo "--- Tech debt (Phase 5): npm ci removed from workflow ---"
WORKFLOW="$PROJECT_ROOT/.github/workflows/hugo.yml"
NPM_CI_COUNT=$(grep -c 'npm ci' "$WORKFLOW" || true)
if [ "$NPM_CI_COUNT" -eq 0 ]; then
  echo "  PASS: no 'npm ci' in .github/workflows/hugo.yml"
  PASS=$((PASS + 1))
else
  echo "  FAIL: 'npm ci' still present in workflow ($NPM_CI_COUNT occurrence(s))"
  FAIL=$((FAIL + 1))
fi
echo ""

# --- Phase 5 gap closure: SUMMARY frontmatter backfill present ---
echo "--- Tech debt (Phase 5): requirements-completed in all target SUMMARYs ---"
BACKFILL_COUNT=0
for SUMMARY in \
  "$PROJECT_ROOT/.planning/phases/01-theme-foundation/01-02-SUMMARY.md" \
  "$PROJECT_ROOT/.planning/phases/01-theme-foundation/01-03-SUMMARY.md" \
  "$PROJECT_ROOT/.planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md" \
  "$PROJECT_ROOT/.planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md"; do
  if grep -q '^requirements-completed:' "$SUMMARY" 2>/dev/null; then
    BACKFILL_COUNT=$((BACKFILL_COUNT + 1))
  fi
done
if [ "$BACKFILL_COUNT" -eq 4 ]; then
  echo "  PASS: requirements-completed present in all 4 target SUMMARY files"
  PASS=$((PASS + 1))
else
  echo "  FAIL: requirements-completed present in only $BACKFILL_COUNT of 4 target SUMMARY files"
  FAIL=$((FAIL + 1))
fi
echo ""
```

The new block adds 6 assertion points (A11Y-01 = 1, PERF-03 preload parity = 2, no-woff2 = 1, npm-ci = 1, backfill = 1). Total PASS count after Plan 01 + Plan 02 land: 33 existing + 6 new = 39 checks, all passing.

Do NOT modify any other existing check. Do NOT touch `set -uo pipefail`. Preserve the final `echo "=== Results: $PASS passed, $FAIL failed ==="` summary and the `exit 1` fallback at the bottom.
  </action>
  <verify>
    <automated>grep -qE "grep -cE 'href=\"https://falkensmage\\\\.com/\\?\"'" tests/validate-phase-03.sh && [ "$(grep -c 'Phase 5' tests/validate-phase-03.sh)" -ge 5 ] && rm -rf public/ resources/_gen/ && hugo --quiet && bash tests/validate-phase-03.sh</automated>
  </verify>
  <acceptance_criteria>
    - Patched SEO-03 line present: `grep -qE "grep -cE 'href=\"https://falkensmage\\\\.com/\\?\"'" tests/validate-phase-03.sh` returns 0
    - Old SEO-03 literal gone: `grep -c "grep -c 'href=\"https://falkensmage.com\"'" tests/validate-phase-03.sh` returns `0`
    - New Phase-5 section headers present: `grep -c 'Phase 5' tests/validate-phase-03.sh` returns at least `5`
    - `bash tests/validate-phase-03.sh` exits `0` after a clean `hugo --quiet` build (requires Plan 01 committed)
    - Final summary line shows `0 failed` and `passed` count `>= 39`
    - Diff shows ONLY the SEO-03 line patched and the new Phase-5 block appended (no other existing checks altered)
  </acceptance_criteria>
  <done>Script reports accurate PASS/FAIL counts. SEO-03 false failure eliminated. Plan 01's A11Y-01 and PERF-03 fixes have continuous structural regression coverage. Tech-debt items (npm ci absence, frontmatter backfill) are validated by the same test run — no separate Phase 4 script needed (per 05-RESEARCH Open Questions Q2).</done>
</task>

<task type="auto">
  <name>Task 2: Delete vestigial npm ci step from .github/workflows/hugo.yml</name>
  <files>.github/workflows/hugo.yml</files>
  <read_first>
    - .github/workflows/hugo.yml (entire file — lines 44–51 are the target region: `Setup Pages` -> `Install Node.js dependencies` -> `Build with Hugo`)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Pattern 4 (rationale + rejected alternatives)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Code Examples Example 4 (exact before/after)
  </read_first>
  <action>
Edit `.github/workflows/hugo.yml`. Delete exactly the two consecutive lines constituting the Node.js install step, plus the blank separator that now becomes redundant.

BEFORE (lines 46–51 of current file):
```
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v6

      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"

      - name: Build with Hugo
```

AFTER:
```
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v6

      - name: Build with Hugo
```

Specifically: remove the `- name: Install Node.js dependencies` line, the following `run: ...` line, and one of the surrounding blank lines so the final output has exactly one blank line between `Setup Pages` and `Build with Hugo`.

Do NOT modify any other step. Do NOT delete `package.json` or `package-lock.json` from the repo — per research Open Questions Q1 and Assumption A2, they remain as version documentation (Cinzel 5.2.8, Space Grotesk 5.2.10). Do NOT touch the `Install Dart Sass` step (line 36–37) — still required by Hugo's extended build.

Do NOT touch the `Build with Hugo` step, the `Upload artifact` step, the `deploy:` job, or any `uses:` action versions.
  </action>
  <verify>
    <automated>[ "$(grep -c 'npm ci' .github/workflows/hugo.yml)" -eq 0 ] && [ "$(grep -c 'Install Node.js dependencies' .github/workflows/hugo.yml)" -eq 0 ] && grep -q 'Setup Pages' .github/workflows/hugo.yml && grep -q 'Build with Hugo' .github/workflows/hugo.yml && grep -q 'Install Dart Sass' .github/workflows/hugo.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'npm ci' .github/workflows/hugo.yml` returns `0`
    - `grep -c 'Install Node.js dependencies' .github/workflows/hugo.yml` returns `0`
    - `grep -q 'Setup Pages' .github/workflows/hugo.yml` exits 0 (step still present)
    - `grep -q 'Build with Hugo' .github/workflows/hugo.yml` exits 0 (step still present)
    - `grep -q 'Install Dart Sass' .github/workflows/hugo.yml` exits 0 (unrelated step preserved)
    - `git diff .github/workflows/hugo.yml` shows only the two-line removal and one blank-line adjustment
    - `package.json` and `package-lock.json` are NOT modified or deleted
  </acceptance_criteria>
  <done>CI workflow is internally consistent per ROADMAP Phase 5 success criterion 4 — `npm ci` removed; committed WOFF2 files under `themes/arcaeon/static/fonts/` are the intentional source of truth. Phase 3 structural check (added in Task 1) continues to assert this going forward.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill 01-02-SUMMARY.md frontmatter with requirements-completed</name>
  <files>.planning/phases/01-theme-foundation/01-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/01-theme-foundation/01-02-PLAN.md (read the `requirements:` field in frontmatter — the REQ-IDs to copy verbatim)
    - .planning/phases/01-theme-foundation/01-02-SUMMARY.md (current frontmatter — lines 1–30; find where to insert)
    - .planning/phases/01-theme-foundation/01-01-SUMMARY.md line 55 (reference pattern: `requirements-completed: [INFRA-01, INFRA-02, THEME-01, THEME-04, THEME-07]`)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Pattern 5 and Code Examples Example 5 (canonical key name + inline array convention)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Common Pitfalls Pitfall 5 (inline array, not multi-line block)
  </read_first>
  <action>
The source of truth is `01-02-PLAN.md`'s `requirements:` field, which lists `THEME-02` and `THEME-03`. Copy those IDs verbatim into `01-02-SUMMARY.md`'s frontmatter as the value of a new `requirements-completed:` key.

Edit `.planning/phases/01-theme-foundation/01-02-SUMMARY.md`. Insert exactly ONE new line in the YAML frontmatter, BEFORE the `metrics:` line (currently line 25).

Insert this line verbatim:
    `requirements-completed: [THEME-02, THEME-03]`

The frontmatter region runs from line 1 (`---`) to the closing `---`. The target insertion point is immediately before `metrics:` (currently at line 25). After the edit, `metrics:` shifts to line 26. All other frontmatter content (phase, plan, subsystem, tags, dependency_graph, tech_stack, key_files, decisions) remains exactly as-is.

Use inline array YAML format on a single line. Do NOT use multi-line block syntax. Do NOT add surrounding blank lines. Match the existing style used in `01-01-SUMMARY.md` line 55.
  </action>
  <verify>
    <automated>grep -q '^requirements-completed: \[THEME-02, THEME-03\]$' .planning/phases/01-theme-foundation/01-02-SUMMARY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^requirements-completed: \[THEME-02, THEME-03\]$' .planning/phases/01-theme-foundation/01-02-SUMMARY.md` returns `1`
    - Line appears inside the YAML frontmatter block (between the two `---` markers), not in the body
    - `metrics:` key is still present in frontmatter
    - No other frontmatter keys removed or reordered
    - Markdown body content below the closing `---` is byte-identical to before
  </acceptance_criteria>
  <done>Milestone audit traceability restored for Phase 1 Plan 2: THEME-02 and THEME-03 now appear in at least one SUMMARY.md `requirements-completed` field, per ROADMAP Phase 5 success criterion 5.</done>
</task>

<task type="auto">
  <name>Task 4: Backfill 01-03-SUMMARY.md frontmatter with requirements-completed</name>
  <files>.planning/phases/01-theme-foundation/01-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/01-theme-foundation/01-03-PLAN.md (read `requirements:` field — lists THEME-04, THEME-05, THEME-06)
    - .planning/phases/01-theme-foundation/01-03-SUMMARY.md (current frontmatter)
    - .planning/phases/01-theme-foundation/01-01-SUMMARY.md line 55 (reference pattern)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Assumptions Log Assumption A5 (THEME-04 intentional overlap — already in 01-01-SUMMARY, also acceptable here)
  </read_first>
  <action>
The source of truth is `01-03-PLAN.md`'s `requirements:` field, which lists `THEME-04`, `THEME-05`, `THEME-06`. Copy those IDs verbatim into `01-03-SUMMARY.md`'s frontmatter.

Edit `.planning/phases/01-theme-foundation/01-03-SUMMARY.md`. Insert exactly ONE new line in the YAML frontmatter, BEFORE the `metrics:` line.

Insert this line verbatim:
    `requirements-completed: [THEME-04, THEME-05, THEME-06]`

THEME-04 is also listed in `01-01-SUMMARY.md`'s `requirements-completed` — this overlap is intentional per research Assumption A5 (dark aesthetic tokenized in Plan 01 and finalized in Plan 03; audit treats requirement as satisfied if listed in ANY plan; duplication is informative, not incorrect).

Use inline array YAML format on a single line. Do NOT use multi-line block syntax. Do NOT add surrounding blank lines. Match the existing style used in `01-01-SUMMARY.md` line 55.
  </action>
  <verify>
    <automated>grep -q '^requirements-completed: \[THEME-04, THEME-05, THEME-06\]$' .planning/phases/01-theme-foundation/01-03-SUMMARY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^requirements-completed: \[THEME-04, THEME-05, THEME-06\]$' .planning/phases/01-theme-foundation/01-03-SUMMARY.md` returns `1`
    - Line appears inside the YAML frontmatter block (between the two `---` markers), not in the body
    - `metrics:` key is still present in frontmatter
    - No other frontmatter keys removed or reordered
    - Markdown body content below the closing `---` is byte-identical to before
  </acceptance_criteria>
  <done>Milestone audit traceability restored for Phase 1 Plan 3: THEME-04, THEME-05, THEME-06 now appear in this SUMMARY's `requirements-completed` field. (THEME-04 duplicated with 01-01 per A5.)</done>
</task>

<task type="auto">
  <name>Task 5: Backfill 03-01-SUMMARY.md frontmatter with requirements-completed</name>
  <files>.planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03-dynamic-layer-quality/03-01-PLAN.md (read `requirements:` field — lists CURR-01, CURR-02, CURR-03, PERF-03, A11Y-01, A11Y-04)
    - .planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md (current frontmatter)
    - .planning/phases/01-theme-foundation/01-01-SUMMARY.md line 55 (reference pattern)
    - .planning/phases/05-v1-gap-closure/05-RESEARCH.md section Pattern 5 and Code Examples Example 5
  </read_first>
  <action>
The source of truth is `03-01-PLAN.md`'s `requirements:` field, which lists exactly these six IDs: `CURR-01`, `CURR-02`, `CURR-03`, `PERF-03`, `A11Y-01`, `A11Y-04`.

Edit `.planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md`. Insert exactly ONE new line in the YAML frontmatter, BEFORE the `metrics:` line.

Insert this line verbatim:
    `requirements-completed: [CURR-01, CURR-02, CURR-03, PERF-03, A11Y-01, A11Y-04]`

Note: PERF-03 and A11Y-01 are ALSO being re-closed by Plan 05-01 of this phase (they were marked `partial` in the audit). Listing them here in 03-01-SUMMARY is correct per the original plan scope — they were intended to be complete after Plan 03-01. Their partial-status was a defect against that intent, which Plan 05-01 fixes.

Use inline array YAML format on a single line. Do NOT use multi-line block syntax.
  </action>
  <verify>
    <automated>grep -q '^requirements-completed: \[CURR-01, CURR-02, CURR-03, PERF-03, A11Y-01, A11Y-04\]$' .planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^requirements-completed: \[CURR-01, CURR-02, CURR-03, PERF-03, A11Y-01, A11Y-04\]$' .planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md` returns `1`
    - Line appears inside the YAML frontmatter block
    - `metrics:` key still present
    - No other frontmatter keys removed or reordered
    - Markdown body content unchanged
  </acceptance_criteria>
  <done>Milestone audit traceability restored for Phase 3 Plan 1: all 6 REQ-IDs from the PLAN now appear in the SUMMARY's `requirements-completed` field.</done>
</task>

<task type="auto">
  <name>Task 6: Backfill 03-02-SUMMARY.md frontmatter with requirements-completed</name>
  <files>.planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03-dynamic-layer-quality/03-02-PLAN.md (read `requirements:` field — lists SEO-01, SEO-02, SEO-03, PERF-01, PERF-02, PERF-04, A11Y-02, A11Y-03)
    - .planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md (current frontmatter)
    - .planning/phases/01-theme-foundation/01-01-SUMMARY.md line 55 (reference pattern)
  </read_first>
  <action>
The source of truth is `03-02-PLAN.md`'s `requirements:` field, which lists exactly these eight IDs: `SEO-01`, `SEO-02`, `SEO-03`, `PERF-01`, `PERF-02`, `PERF-04`, `A11Y-02`, `A11Y-03`.

Edit `.planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md`. Insert exactly ONE new line in the YAML frontmatter, BEFORE the `metrics:` line.

Insert this line verbatim:
    `requirements-completed: [SEO-01, SEO-02, SEO-03, PERF-01, PERF-02, PERF-04, A11Y-02, A11Y-03]`

Note: PERF-01 is also being closed by Plan 05-02 of this phase (it was marked `partial` in the audit, as a derivative of PERF-03). Listing it here in 03-02-SUMMARY is correct per the original plan scope — it was intended to be complete after Plan 03-02. Its partial-status was a defect which Plan 05 closes end-to-end.

Use inline array YAML format on a single line. Do NOT use multi-line block syntax.
  </action>
  <verify>
    <automated>grep -q '^requirements-completed: \[SEO-01, SEO-02, SEO-03, PERF-01, PERF-02, PERF-04, A11Y-02, A11Y-03\]$' .planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^requirements-completed: \[SEO-01, SEO-02, SEO-03, PERF-01, PERF-02, PERF-04, A11Y-02, A11Y-03\]$' .planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md` returns `1`
    - Line appears inside the YAML frontmatter block
    - `metrics:` key still present
    - No other frontmatter keys removed or reordered
    - Markdown body content unchanged
  </acceptance_criteria>
  <done>Milestone audit traceability restored for Phase 3 Plan 2: all 8 REQ-IDs from the PLAN now appear in the SUMMARY's `requirements-completed` field. Combined with Tasks 3, 4, 5: every v1.0 REQ-ID now appears in at least one SUMMARY.md across Phases 1–4 — ROADMAP Phase 5 success criterion 5 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. Plan 02 modifies build-tier scripts (`validate-phase-03.sh`), CI workflow YAML (`.github/workflows/hugo.yml`), and planning-tier documentation (SUMMARY.md files). No runtime code changes, no attack surface added.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | CI workflow step removal alters deploy behavior | accept | `npm ci` removal documented in 05-RESEARCH Pattern 4; audit criterion 4 explicitly calls for this simplification; committed WOFF2 files are the trust root going forward; structural check in Task 1 prevents accidental reintroduction |

Per 05-RESEARCH.md section Security Domain: no new attack surface — static-content, workflow-config, and documentation edits only. STRIDE categories S/R/I/D/E are not in scope.
</threat_model>

<verification>
Plan-wide verification after all six tasks land:

```bash
# Task 1 — script patch + new checks
rm -rf public/ resources/_gen/
hugo --quiet
bash tests/validate-phase-03.sh  # must exit 0 with PASS >= 39, FAIL = 0

# Task 2 — npm ci gone
[ "$(grep -c 'npm ci' .github/workflows/hugo.yml)" -eq 0 ]

# Tasks 3–6 — all four SUMMARY files have the new key
for f in \
  .planning/phases/01-theme-foundation/01-02-SUMMARY.md \
  .planning/phases/01-theme-foundation/01-03-SUMMARY.md \
  .planning/phases/03-dynamic-layer-quality/03-01-SUMMARY.md \
  .planning/phases/03-dynamic-layer-quality/03-02-SUMMARY.md; do
  grep -q '^requirements-completed:' "$f" || echo "MISSING: $f"
done
# Expected output: (empty)
```

All six tasks verified in one run via Task 1's own new `Tech debt (Phase 5): requirements-completed in all target SUMMARYs` check.
</verification>

<success_criteria>
1. `tests/validate-phase-03.sh` line 154 uses `grep -cE` with trailing-slash-optional regex — SEO-03 passes cleanly on Hugo's actual output
2. `tests/validate-phase-03.sh` contains 5 new Phase-5 check sections (6 assertion points total) covering A11Y-01, PERF-03 URL parity for both fonts, no `.woff2` in `public/css/`, `npm ci` absent, frontmatter backfill present
3. `bash tests/validate-phase-03.sh` exits 0 after clean build — ROADMAP Phase 5 success criterion 3
4. `.github/workflows/hugo.yml` contains zero occurrences of `npm ci` — ROADMAP Phase 5 success criterion 4
5. All four target SUMMARY.md files contain `requirements-completed: [...]` with IDs copied verbatim from the corresponding PLAN's `requirements:` field
6. `git diff` shows no unintended changes (no other frontmatter reordering, no body content modified)
</success_criteria>

<output>
After completion, create `.planning/phases/05-v1-gap-closure/05-02-SUMMARY.md`.

Required frontmatter:
- `phase: 05-v1-gap-closure`
- `plan: 02`
- `requirements-completed: [PERF-01]`
- `key_files.modified:` lists all six edited paths
</output>
