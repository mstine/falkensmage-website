---
phase: 06-offer-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hugo.toml
  - themes/arcaeon/layouts/work/single.html
  - themes/arcaeon/layouts/work/list.html
autonomous: true
requirements:
  - OFFER-05
  - OFFERQ-01
  - OFFERQ-02
user_setup: []

must_haves:
  truths:
    - "hugo.toml has a [params.booking] table with three keys (the-query, the-cast, the-daemon), each a placeholder Cal.com URL"
    - "themes/arcaeon/layouts/work/single.html exists and renders an offer page from the _default/baseof.html shell"
    - "themes/arcaeon/layouts/work/list.html exists and renders the /work/ hub from the _default/baseof.html shell"
    - "The single template CTA href resolves from site.Params.booking indexed by the page booking_key front-matter field — no hardcoded booking URL in the template"
    - "The CTA button uses class glow-radiant-core and links out with target=_blank rel=noopener noreferrer (external-link branch of the identity-cta convention)"
    - "Templates compose only from existing arcaeon classes (section, section-void, section-depth, section-content, glow-radiant-core, currently-card/social-grid, sigil-*) — no new CSS rules and no style blocks"
    - "hugo --minify builds clean (exit 0) with these templates present"
  artifacts:
    - path: "hugo.toml"
      provides: "Single-source-of-truth [params.booking] table for the three Cal.com link-out URLs (OFFER-05)"
      contains: "[params.booking]"
    - path: "themes/arcaeon/layouts/work/single.html"
      provides: "Offer-page template — voiced pitch body + price + Radiant-Core link-out CTA driven by booking_key"
      contains: "glow-radiant-core"
    - path: "themes/arcaeon/layouts/work/list.html"
      provides: "Work-With-Me hub template — framing + offer cards linking to each offer page"
      contains: "range"
  key_links:
    - from: "offer page front-matter booking_key"
      to: "hugo.toml [params.booking] table"
      via: "index site.Params.booking .Params.booking_key in work/single.html"
      pattern: "index .*[Bb]ooking"
  prohibitions:
    - "No hardcoded booking/Cal.com URL literal in either work/ template file (must dereference the config key)"
    - "No style block, no new CSS class definitions, no external script or booking-widget embed in either template"
    - "No target=_blank without rel=noopener (or rel=noopener noreferrer)"
---

<objective>
Lay the plumbing for the `/work/` commerce section: centralize the three Cal.com booking URLs as single-source-of-truth config, and build the two arcaeon-native section templates (hub list + offer single) that all Phase 6 content renders through. No content and no homepage change here — just config + templates.

This plan makes OFFER-05 real (one place to swap live URLs later, per OFFER-F1) and enforces OFFERQ-01 (design-system reuse only) and OFFERQ-02 (link-out, zero external deps) structurally in the templates so the later content plan cannot violate them.

Purpose: The templates are the guardrail — they dereference a config key for the CTA target (never a hardcoded URL) and compose exclusively from existing arcaeon classes, so budget and design compliance are baked into the render path.
Output: `[params.booking]` in hugo.toml; `themes/arcaeon/layouts/work/single.html`; `themes/arcaeon/layouts/work/list.html`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-offer-pages/06-CONTEXT.md

@hugo.toml
@themes/arcaeon/layouts/_default/baseof.html
@themes/arcaeon/layouts/kitchen-sink/single.html
@themes/arcaeon/layouts/partials/sections/identity-cta.html
@themes/arcaeon/layouts/index.html

<interfaces>
**Cal.com config shape (OFFER-05).** Add to `hugo.toml` under `[params]` (block starts line 6, before the `[params.colors]` table). Use a `[params.booking]` table with exactly three keys matching each offer's `booking_key`:
- `the-query` — placeholder for the tarot reading booking flow
- `the-cast` — placeholder for the astrology reading booking flow
- `the-daemon` — placeholder for the clarity-consult booking flow
Placeholder values are non-live Cal.com URLs (a `https://cal.com/PLACEHOLDER/...` form). The point is a template dereferences `index site.Params.booking .Params.booking_key`; the literal URL lives ONLY here.

**Template shell + composition.** Both templates use `{{ define "main" }}...{{ end }}` and render inside `<main>` on `_default/baseof.html` (same pattern as `kitchen-sink/single.html`). Section wrappers are `<section class="section section-void">` / `section-depth`, inner `<div class="section-content">`. Alternate void/depth between sections (D-11). Apply the Triad Rule per section (one purple + one blue + one warm accent) using existing palette CSS vars — no new colors.

**CTA pattern (follow identity-cta.html).** The external Cal.com link is the `cta_url` branch of `identity-cta.html`: an anchor with class `glow-radiant-core`, `target="_blank"`, `rel="noopener noreferrer"`. The mailto branch (no `target=_blank`) does NOT apply here — Cal.com is an external https destination, so the external-link branch (with `rel="noopener noreferrer"`) is correct. Href value = `{{ index site.Params.booking .Params.booking_key }}`.

**Offer page front-matter contract (consumed by single.html; authored in Plan 06-02).** Each offer page provides: `title`, `offer_name`, `price`, `duration` (Daemon may omit/adapt), `cta_text`, `booking_key`, `summary`, `weight`. The single template reads these params; the markdown body is the voiced pitch rendered via `{{ .Content }}`.

**Hub contract (consumed by list.html; content authored in Plan 06-02).** `content/work/_index.md` provides the short framing body (`{{ .Content }}`). The hub cards iterate the section offer pages via `{{ range .Pages.ByWeight }}` (or `.Pages` sorted), pulling each page's `offer_name`, `title`, `price`, `summary`, and `.RelPermalink` for the internal link.

**Reuse catalog (OFFERQ-01 — no new CSS).** Available classes (from `themes/arcaeon/assets/css/main.css`): `section`, `section-void`, `section-depth`, `section-content`, `display-text`, `glow-radiant-core`, `glow-interactive`, `currently-card` (border-only ghost card, hover border-color — ideal for offer cards), `social-grid` + `social-card` (compact card grid), `sigil-arc`, `sigil-arc-sm`, `sigil-gradient`, `sigil-boundary-tr`, `sigil-boundary-bl`. Use these; do not author new selectors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add [params.booking] single-source config to hugo.toml (OFFER-05)</name>
  <files>hugo.toml</files>
  <read_first>
    - hugo.toml (entire file — the [params] block opens line 6; [params.colors] starts line 11; [caches] at line 24)
    - .planning/phases/06-offer-pages/06-CONTEXT.md section "Cal.com config (single source of truth) — OFFER-05"
  </read_first>
  <action>
Add a `[params.booking]` TOML table to `hugo.toml` inside the `[params]` namespace. Place it after the `author`/`description`/`plausible` keys and before the `[params.colors]` table (insert a new table block between line 9 and line 11), so it reads as a sibling sub-table of `[params]`.

The table has exactly three keys — `the-query`, `the-cast`, `the-daemon` — each set to a clearly-marked PLACEHOLDER Cal.com URL (a `https://cal.com/PLACEHOLDER/<slug>` form; the-daemon's slug should signal the clarity consult, e.g. `the-daemon-consult`). Add a short comment noting these are placeholders swapped in one place under OFFER-F1 when Cal.com event types exist.

Because two keys contain a hyphen, quote them as TOML string keys (`"the-query" = "..."`). Do NOT hardcode these URLs anywhere else — templates dereference `site.Params.booking`. Leave every other line of hugo.toml (baseURL, theme, colors, caches) byte-identical.
  </action>
  <verify>
    <automated>hugo config 2>/dev/null | grep -iA4 booking && hugo --minify --quiet && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `hugo.toml` contains a `[params.booking]` table with keys `the-query`, `the-cast`, `the-daemon`
    - `hugo config` output shows the three booking keys under the params/booking namespace
    - Each value is a placeholder Cal.com URL string (no live event-type link yet)
    - `[params.colors]` and `[caches]` tables are unchanged; `baseURL`/`title`/`theme` unchanged
    - `hugo --minify --quiet` exits 0 with the new table present
  </acceptance_criteria>
  <done>The three booking URLs live in exactly one place. Any template can resolve a CTA target by `index site.Params.booking <key>`, and the future live-URL swap (OFFER-F1) is a three-line edit in this one table.</done>
</task>

<task type="auto">
  <name>Task 2: Build the offer-page template themes/arcaeon/layouts/work/single.html</name>
  <files>themes/arcaeon/layouts/work/single.html</files>
  <read_first>
    - themes/arcaeon/layouts/kitchen-sink/single.html (the section-composition pattern to mirror: define "main", alternating section-void/section-depth, section-content wrappers)
    - themes/arcaeon/layouts/partials/sections/identity-cta.html (the exact Radiant-Core external-link CTA markup: class glow-radiant-core, target=_blank, rel=noopener noreferrer)
    - themes/arcaeon/layouts/_default/baseof.html (the shell; confirm non-home title uses .Title)
    - themes/arcaeon/assets/css/main.css lines 291-336 (.glow-radiant-core) and 704-725 (.currently-card)
  </read_first>
  <action>
Create `themes/arcaeon/layouts/work/single.html` — the template every individual offer page (`the-query`, `the-cast`, `the-daemon`) renders through. Wrap everything in `{{ define "main" }}<main>...</main>{{ end }}`.

Compose three alternating sections (D-11 void/depth alternation), each an existing arcaeon `section` with a `section-content` inner wrapper. Apply the Triad Rule per section (one purple + one blue + one warm accent) using existing palette CSS custom properties only:

1. Hero section (`section-void`): eyebrow line = `.Params.offer_name`; page title in Cinzel display (an `h1` or the `display-text` class per the kitchen-sink type scale); a price/shape line combining `.Params.price` and `.Params.duration` — render duration only inside `{{ with .Params.duration }}` since the Daemon may omit it. Add a subtle sigil decoration via an existing sigil class (e.g. `sigil-arc` or a `sigil-boundary-*` modifier), decorative only.
2. Pitch section (`section-depth`): render the voiced pitch markdown body with `{{ .Content }}`. This is where Plan 06-02 prose lands — pass it through unmolested.
3. CTA section (`section-void`): the conversion moment. Render an anchor with class `glow-radiant-core` that opens the booking flow in a new browser tab, carrying the SAME external-link safety attributes as the identity-cta `cta_url` branch (a `rel` of `noopener noreferrer`); link text = `.Params.cta_text`, and href resolved from the booking config. The href MUST dereference the config — do not write any booking-provider domain or URL literal into this file. For the consult-gated Daemon framing you may echo `.Params.price` near the CTA.

Guard the CTA so a missing `booking_key` or empty resolved URL does not emit a broken empty href: wrap the anchor in `{{ with index site.Params.booking .Params.booking_key }}...{{ end }}` and use the bound value as the href. Use ONLY existing arcaeon classes and palette vars. Do not add an inline style element, do not define new CSS selectors, and do not add any external script tag or booking embed.
  </action>
  <verify>
    <automated>test -f themes/arcaeon/layouts/work/single.html && grep -q 'glow-radiant-core' themes/arcaeon/layouts/work/single.html && grep -qE 'index .*[Bb]ooking' themes/arcaeon/layouts/work/single.html && grep -q 'rel="noopener' themes/arcaeon/layouts/work/single.html && [ "$(grep -Ec 'cal\.com|https?://[^ ]*book' themes/arcaeon/layouts/work/single.html)" -eq 0 ] && [ "$(grep -c '<style' themes/arcaeon/layouts/work/single.html)" -eq 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `themes/arcaeon/layouts/work/single.html` exists and defines `main`
    - Contains three alternating `section-void`/`section-depth` sections with `section-content` wrappers
    - Renders `{{ .Content }}` for the pitch body
    - CTA anchor has class `glow-radiant-core`, `target="_blank"`, `rel="noopener noreferrer"`, and text `{{ .Params.cta_text }}`
    - CTA href dereferences `index site.Params.booking .Params.booking_key` — grep for `index .*[Bb]ooking` matches; grep for a booking-provider URL literal returns 0
    - No `<style>` block and no new CSS selectors; no external `<script>`/embed
  </acceptance_criteria>
  <done>Any offer page dropping the front-matter contract renders as a voiced pitch with a working, config-driven, budget-safe link-out CTA. The template alone guarantees OFFERQ-01/OFFERQ-02 compliance for offer pages.</done>
</task>

<task type="auto">
  <name>Task 3: Build the hub template themes/arcaeon/layouts/work/list.html (OFFER-01)</name>
  <files>themes/arcaeon/layouts/work/list.html</files>
  <read_first>
    - themes/arcaeon/layouts/kitchen-sink/single.html (composition pattern)
    - themes/arcaeon/layouts/partials/sections/social.html (the .Pages/range + card-grid idiom used elsewhere in this theme)
    - themes/arcaeon/assets/css/main.css lines 579-640 (.social-grid/.social-card) and 704-725 (.currently-card) — the card classes to reuse for offer cards
  </read_first>
  <action>
Create `themes/arcaeon/layouts/work/list.html` — the `/work/` hub. Wrap in `{{ define "main" }}<main>...</main>{{ end }}`.

Two sections, alternating (D-11), Triad Rule per section, existing classes only:

1. Framing section (`section-void`): render the short hub framing from `content/work/_index.md` via `{{ .Content }}` (Plan 06-02 authors the copy — keep it a front door, not an essay). Include the section title from `.Title`.
2. Offers section (`section-depth`): iterate the three offer pages with `{{ range .Pages.ByWeight }}` and render one card per offer. Each card links to the offer via `.RelPermalink` (internal same-site navigation — it opens in the SAME tab, so do not add a new-tab attribute) and surfaces `.Params.offer_name`, `.Title`, `.Params.price`, and `.Params.summary`. Reuse an existing card class for the visual: `currently-card` (ghost card, hover border-color) works well for a stacked list of three, or `social-grid` + a card class for a grid. Use `glow-interactive` on the card link for the hover treatment if desired. Do NOT author new CSS.

`.Pages.ByWeight` returns the section regular pages (the three offers) but excludes `_index.md`, so the hub will not list itself. Confirm the range yields exactly the three offer pages once Plan 06-02 content exists. Do not add an inline style element and do not define new selectors.
  </action>
  <verify>
    <automated>test -f themes/arcaeon/layouts/work/list.html && grep -qE 'range .*\.Pages' themes/arcaeon/layouts/work/list.html && grep -q 'RelPermalink' themes/arcaeon/layouts/work/list.html && grep -q '.Content' themes/arcaeon/layouts/work/list.html && [ "$(grep -c '<style' themes/arcaeon/layouts/work/list.html)" -eq 0 ] && [ "$(grep -c 'target="_blank"' themes/arcaeon/layouts/work/list.html)" -eq 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `themes/arcaeon/layouts/work/list.html` exists and defines `main`
    - Renders hub framing via `{{ .Content }}` and section `.Title`
    - Ranges over `.Pages` (`.Pages.ByWeight` or sorted) and emits one card per offer linking to `.RelPermalink`
    - Each card surfaces offer_name/title/price/summary
    - Cards reuse an existing arcaeon card class (`currently-card` or `social-card`); no new selectors, no `<style>` block
    - Internal card links do NOT use `target="_blank"` (same-site navigation)
  </acceptance_criteria>
  <done>The hub is a working front door: it renders its own framing and lists all three offers as arcaeon-native cards linking to each offer page. Rendering is verified end-to-end once Plan 06-02 supplies the content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site visitor → Cal.com | The only outbound crossing. A CTA `<a href>` sends the visitor to an external, hosted Cal.com booking/checkout flow. No widget, no JS, no form, no user input captured on-site, no DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | Cal.com link-out target (typosquat / wrong domain) | medium | mitigate | Booking URLs live in ONE `[params.booking]` table (OFFER-05); reviewer verifies the three placeholders (and the live swap under OFFER-F1) against the real Cal.com workspace domain in a single place instead of scattered literals. Templates cannot introduce a divergent URL — they dereference the key. |
| T-06-02 | Information Disclosure | Reverse-tabnabbing via `target="_blank"` | low | mitigate | CTA follows the identity-cta external-link branch: `rel="noopener noreferrer"` is mandatory on every `target="_blank"` anchor. Acceptance grep asserts `rel="noopener"` present. |
| T-06-03 | Tampering | Supply-chain / external script injection | low | accept | Phase adds zero dependencies, zero external `<script>`, no package installs (OFFERQ-02). Static Hugo templates only; nothing to audit. No package-legitimacy gate applies. |
</threat_model>

<verification>
```bash
# Clean build with the new plumbing
rm -rf public/ resources/_gen/
hugo --minify --quiet

# Config single-source
hugo config | grep -iA4 booking          # three keys present

# Templates exist and are config-driven / budget-safe
grep -q 'glow-radiant-core' themes/arcaeon/layouts/work/single.html
grep -qE 'index .*[Bb]ooking' themes/arcaeon/layouts/work/single.html
grep -q 'rel="noopener' themes/arcaeon/layouts/work/single.html
grep -c '<style' themes/arcaeon/layouts/work/single.html   # 0
grep -c '<style' themes/arcaeon/layouts/work/list.html     # 0
```
Note: `/work/` pages will not render into `public/` until Plan 06-02 supplies content; this plan verifies the config + template artifacts and a clean build.
</verification>

<success_criteria>
1. `hugo.toml` has a `[params.booking]` table with the three placeholder URLs (OFFER-05).
2. `work/single.html` renders a three-section arcaeon offer page with a config-driven Radiant-Core link-out CTA (OFFERQ-01, OFFERQ-02).
3. `work/list.html` renders the hub framing + ranges offer cards to each offer page (OFFER-01 template half).
4. Neither template contains a hardcoded booking URL, a `<style>` block, new selectors, or an external script.
5. `hugo --minify` builds clean (exit 0).
</success_criteria>

<output>
Create `.planning/phases/06-offer-pages/06-01-SUMMARY.md` when done. Frontmatter must include `requirements-completed: [OFFER-05]` (OFFERQ-01/OFFERQ-02 are structurally enforced here but finally verified in Plan 06-03) and list the three modified/created files under `key_files`.
</output>
