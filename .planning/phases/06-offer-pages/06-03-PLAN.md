---
phase: 06-offer-pages
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - content/_index.md
  - tests/validate-phase-06.sh
autonomous: true
requirements:
  - NAV-01
  - OFFERQ-01
  - OFFERQ-02
user_setup: []

must_haves:
  truths:
    - "content/_index.md cta_secondary front-matter points the homepage secondary CTA to the /work/ hub (no longer a mailto)"
    - "The cta_secondary mechanism is preserved (still a front-matter string rendered by identity-cta.html) — only the target changed"
    - "tests/validate-phase-06.sh exists, is executable, and exits 0 with FAIL == 0 on a clean build"
    - "The validation script asserts: hub + three offer pages render, canonical prices/CTAs present, homepage links to /work/, [params.booking] single-source config, no hardcoded booking URLs in templates/content, no external booking widget/script, rel=noopener on link-out CTAs, design-system reuse (section-void/section-depth + glow-radiant-core), clean hugo --minify build"
    - "The validation script covers all four OFFERQ-02 budget legs: zero-dep/link-out by direct assertion, mobile-first via viewport-meta check on all /work/ pages, and WCAG-AA/375px-fluidity via no-new-hex + no-fixed-px guards (inherited-by-reuse, documented in the script header)"
    - "The-daemon consult-gate is enforced by a concrete negative check: its glow-radiant-core CTA anchor text is not $4,500, Buy Now, or Book & Pay"
    - "hugo --minify builds clean (exit 0); public/ contains zero third-party/booking script or widget embeds"
  artifacts:
    - path: "content/_index.md"
      provides: "Homepage secondary CTA repointed to /work/ (NAV-01)"
      contains: "/work/"
    - path: "tests/validate-phase-06.sh"
      provides: "Automated Phase 6 acceptance regression (pass/fail counter, exit 0 = green) following the tests/validate-phase-NN.sh convention"
      contains: "PASS"
  key_links:
    - from: "homepage secondary CTA (content/_index.md cta_secondary)"
      to: "/work/ hub"
      via: "identity-cta.html rendering cta_secondary as safeHTML"
      pattern: "/work/"
  prohibitions:
    - "Do not change the cta_secondary rendering mechanism (identity-cta.html) — repoint the target only"
    - "Do not remove the Plausible tag or introduce any external booking widget/script into the build"
    - "The validation script must not hardcode-pass — every check reflects a real assertion against built output or source"
---

<objective>
Wire the front door and lock the phase with a regression gate. Repoint the homepage secondary CTA from the bare `mailto:` to the `/work/` hub (NAV-01, preserving the existing front-matter mechanism), then write `tests/validate-phase-06.sh` — the automated acceptance script following this repo's established `tests/validate-phase-NN.sh` convention — and confirm a clean `hugo --minify` build with all budgets held (OFFERQ-01, OFFERQ-02).

Purpose: NAV-01 makes the offers reachable from the front door. The validation script is the durable proof that all five ROADMAP Phase 6 success criteria hold on a clean build, and the guard against future regressions.
Output: repointed `content/_index.md` `cta_secondary`; new `tests/validate-phase-06.sh`; green clean-build transcript.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-offer-pages/06-CONTEXT.md
@.planning/phases/06-offer-pages/06-01-SUMMARY.md
@.planning/phases/06-offer-pages/06-02-SUMMARY.md

@content/_index.md
@themes/arcaeon/layouts/partials/sections/identity-cta.html
@tests/validate-phase-02.sh
@tests/validate-phase-03.sh

<interfaces>
**NAV-01 repoint (content/_index.md, ~line 22).** Current front matter:
`cta_secondary: "Want to work together? <a href=\"mailto:falkensmage@falkenslabyrinth.com\">falkensmage@falkenslabyrinth.com</a>"`

Repoint the `<a href>` target to the `/work/` hub while KEEPING the `cta_secondary` front-matter field and its raw-HTML `<a>` mechanism (rendered `| safeHTML` by `identity-cta.html` line 22-24). Update the anchor text to a voiced "work together" invite that reads correctly for an internal offers link (not an email address as link text). Use a root-relative `/work/` href (internal nav — do NOT add `target="_blank"`, matching the internal-link convention). Change ONLY this line; leave all other homepage front matter (identity blocks, cta_url, social_links, rewired_launch) byte-identical.

**Validation script convention (tests/validate-phase-06.sh).** Mirror `tests/validate-phase-02.sh` / `03.sh` structure exactly: `#!/usr/bin/env bash`, `set -uo pipefail`, `PASS`/`FAIL` counters, `pass()`/`fail()` helpers, `PROJECT_ROOT` derivation, a clean build up front (`rm -rf public/ resources/_gen/ && hugo --minify --quiet`), grouped echo headers, and a final summary that exits non-zero if `FAIL > 0`. Run it from project root.

**What the script must assert (map to Phase 6 success criteria):**
1. OFFER-01 — `public/work/index.html` exists and links to all three offer pages (`/work/the-query/`, `/work/the-cast/`, `/work/the-daemon/`).
2. OFFER-02/03/04 — each offer page renders: `public/work/the-query/index.html` has `$250` + `90 minutes` + a "Book & Pay" CTA; the-cast has `$325` + "Book & Pay"; the-daemon has `$4,500` + a consult CTA mentioning `$150`/credited.
3. OFFER-05 — `hugo.toml` has `[params.booking]` with the three keys; each offer's built CTA href equals the corresponding placeholder value from that table (config is the single source — content/templates carry no booking URL literal).
4. NAV-01 — `public/index.html` secondary CTA links to `/work/` and no longer contains the old `mailto:falkensmage@falkenslabyrinth.com`.
5. OFFERQ-01 — offer pages use arcaeon classes (`section-void`/`section-depth`, `glow-radiant-core`); no `<style>` blocks in `work/` templates.
6. OFFERQ-02 — link-out only: no `<script>` referencing a booking provider and no Cal.com embed/widget in `public/work/*`; every CTA using `target="_blank"` carries `rel="noopener"`; no new external CDN/dependency added (Plausible, already present in v1.0, is the only external script and is unchanged).
7. Build — clean `hugo --minify` exits 0.

**Language rule check (belt-and-suspenders).** Assert `grep -Eic 'tarot reader|chaos magician'` over `content/work/*.md` returns 0.

**Regression guard.** The script should also run (or the plan should confirm) that `tests/validate-phase-01.sh` and `02.sh` still pass, so the offer section did not regress the homepage/theme.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint homepage secondary CTA to /work/ (NAV-01)</name>
  <files>content/_index.md</files>
  <read_first>
    - content/_index.md (front matter; cta_secondary is line 22 — a raw-HTML anchor string)
    - themes/arcaeon/layouts/partials/sections/identity-cta.html lines 22-24 (confirms cta_secondary is rendered as safeHTML — the mechanism to preserve)
    - .planning/phases/06-offer-pages/06-CONTEXT.md "Homepage CTA repoint — NAV-01"
  </read_first>
  <action>
Edit exactly one line of `content/_index.md`: the `cta_secondary` front-matter value (~line 22). Replace the `mailto:` anchor with an internal link to the `/work/` hub, preserving the field name and the raw-HTML `<a>` mechanism (it is rendered `| safeHTML`).

The new value keeps the "want to work together?" invitation but links to `/work/` and uses voiced anchor text (e.g. an anchor reading like "See how we can work together" or "Work with me") — NOT an email address as the visible link text. Use a root-relative href `/work/`. Because the value is a double-quoted YAML string containing escaped `\"` quotes, keep the same escaping style as the original line. Do not add `target="_blank"` (internal navigation). Change no other line of the file.
  </action>
  <verify>
    <automated>rm -rf public/ resources/_gen/ && hugo --minify --quiet && grep -q 'href="/work/"' public/index.html && [ "$(grep -c 'mailto:falkensmage@falkenslabyrinth.com' public/index.html)" -eq 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `content/_index.md` `cta_secondary` links to `/work/` (root-relative), field name and safeHTML anchor mechanism preserved
    - Rendered `public/index.html` contains `href="/work/"` in the secondary CTA
    - Old `mailto:falkensmage@falkenslabyrinth.com` no longer present in `public/index.html`
    - Anchor text is a voiced work-together invite, not a raw email address
    - No `target="_blank"` on the internal `/work/` link; all other homepage front matter unchanged
  </acceptance_criteria>
  <done>The front door reaches the offers: the homepage secondary CTA points to `/work/` instead of an email. NAV-01 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/validate-phase-06.sh and confirm clean-build green (OFFERQ-01, OFFERQ-02)</name>
  <files>tests/validate-phase-06.sh</files>
  <read_first>
    - tests/validate-phase-02.sh (the full structure to mirror: shebang, set -uo pipefail, PASS/FAIL counters, pass()/fail() helpers, PROJECT_ROOT, grouped headers, final exit)
    - tests/validate-phase-03.sh (clean-build + built-HTML assertion patterns)
    - hugo.toml (confirm the [params.booking] keys/values to compare CTA hrefs against)
    - themes/arcaeon/layouts/work/single.html and work/list.html (the design-reuse assertions target these)
    - content/work/the-query.md, the-cast.md, the-daemon.md (canonical price/CTA values to assert)
  </read_first>
  <action>
Create `tests/validate-phase-06.sh`, executable (`chmod +x`), following the exact structure of `tests/validate-phase-02.sh`. Start with a clean build (`rm -rf public/ resources/_gen/ && hugo --minify --quiet`) and increment a PASS/FAIL counter per check; exit non-zero if any FAIL.

Assertions (each a discrete pass/fail), covering all Phase 6 success criteria:
- OFFER-01: `public/work/index.html` exists and contains links to `/work/the-query/`, `/work/the-cast/`, `/work/the-daemon/`.
- OFFER-02: `public/work/the-query/index.html` contains `$250`, `90 minutes`, and a "Book & Pay" CTA (allow HTML-escaped `Book &amp; Pay`).
- OFFER-03: `public/work/the-cast/index.html` contains `$325`, `90 minutes`, "Book & Pay".
- OFFER-04: `public/work/the-daemon/index.html` contains `$4,500` (the engagement shape) somewhere on the page AND a consult CTA mentioning `$150`/credited. Enforce consult-gating with a CONCRETE negative check, not a vague one: assert the built page contains NO `glow-radiant-core` CTA anchor whose visible link text is `$4,500`, `Buy Now`, or `Book & Pay` — i.e. extract the CTA anchor's inner text and confirm it equals the consult label, not a direct-buy label. (The exact `cta_text` match `Book a Clarity Consult ($150, credited)` is the positive assertion; these three forbidden CTA labels are the negative assertion.)
- OFFER-05: `hugo.toml` has `[params.booking]` with `the-query`/`the-cast`/`the-daemon`; each offer's built CTA `href` equals the matching placeholder from that table (extract the value from `hugo config` or hugo.toml and compare to the href in the built page).
- NAV-01: `public/index.html` has `href="/work/"` in the secondary CTA and contains no legacy mailto contact address (the old falkenslabyrinth.com email link removed by Task 1 — see that task's acceptance grep for the exact string).
- OFFERQ-01: `public/work/the-query/index.html` uses `section-void` AND `section-depth` AND `glow-radiant-core`; `themes/arcaeon/layouts/work/single.html` and `list.html` contain zero `<style` blocks.
- OFFERQ-02 (link-out / zero-dep leg): no `<script` referencing a booking provider anywhere under `public/work/`; no booking-widget/embed markup under `public/work/`; every `target="_blank"` anchor in built offer pages carries `rel="noopener"`; templates/content contain no hardcoded booking-provider URL literal (target comes from config).
- OFFERQ-02 (mobile-first 375px leg): assert every built `/work/` page carries the viewport meta tag (`name="viewport"` with `width=device-width`) — the mobile-first prerequisite, inherited from `baseof.html`. Check `public/work/index.html`, `public/work/the-query/index.html`, `public/work/the-cast/index.html`, `public/work/the-daemon/index.html`. (Prior scripts have no dedicated viewport grep; add one here.)
- OFFERQ-02 (WCAG-AA + fluidity by construction): assert the NEW templates and content introduce no design regressions that would break inherited contrast/fluidity — namely (a) zero hardcoded 6-digit hex color literals in `themes/arcaeon/layouts/work/*.html` and `content/work/*.md` (colors must come from ARCÆON palette CSS vars, so AA contrast is inherited unchanged), and (b) zero fixed-width container declarations (`width:` in px or `min-width:` in px on a container) introduced by the new templates (so the 375px fluid layout is inherited unchanged). These are cheap negative greps proving "no new design language" at the budget level.
- Language rule: assert the two banned practitioner terms do not appear in `content/work/*.md` (reuse the case-insensitive negative grep established in Plan 06-02's verify blocks; count must be 0).
- Build: the clean `hugo --minify` at the top exited 0 (guard with a check that `public/index.html` exists).

Finish by printing the PASS/FAIL summary and exiting 0 only when FAIL == 0. After writing, RUN it and also run `tests/validate-phase-01.sh` and `tests/validate-phase-02.sh` as a regression guard; all three must be green.

Note on the booking-URL-literal negative grep: target the template files and `content/work/*.md` (the config value in `hugo.toml` legitimately contains the placeholder — exclude `hugo.toml` from that particular negative check).

Note on OFFERQ-02 coverage rationale (document in the script header comment): OFFERQ-02 is a four-part budget — sub-1s/3G, zero external deps/CDN, mobile-first 375px, WCAG AA. This phase adds NO new CSS system: templates compose from existing arcaeon classes and palette vars only. Therefore sub-1s/3G, mobile-first fluidity, and WCAG-AA contrast are INHERITED-BY-REUSE from the v1.0 theme (already verified in phases 1–3) rather than re-measured here. The viewport-meta check and the no-new-hex / no-fixed-px checks above are the guards that keep that inheritance intact — if the new work touched the design system, they fail. Full 3G-timing and contrast re-measurement is out of scope precisely because no new design surface was introduced.
  </action>
  <verify>
    <automated>chmod +x tests/validate-phase-06.sh && bash tests/validate-phase-06.sh && bash tests/validate-phase-01.sh && bash tests/validate-phase-02.sh && echo ALLGREEN</automated>
  </verify>
  <acceptance_criteria>
    - `tests/validate-phase-06.sh` exists, is executable, mirrors the `validate-phase-02.sh` structure (PASS/FAIL counters, clean build, final exit)
    - Running it exits 0 with FAIL == 0 and a non-trivial PASS count covering all eight requirement IDs
    - Assertions include: hub links to 3 offers; canonical prices `$250`/`$325`/`$4,500`; correct CTAs; homepage `href="/work/"` and no old mailto; `[params.booking]` single-source with hrefs matching config; design reuse (`section-void`/`section-depth`/`glow-radiant-core`, no `<style>` in work templates); no booking script/widget under `public/work/`; `rel="noopener"` on any `target="_blank"` CTA; language rule 0
    - OFFER-04 consult-gate enforced by concrete negative check: the-daemon `glow-radiant-core` CTA anchor text is NOT `$4,500`, `Buy Now`, or `Book & Pay`
    - OFFERQ-02 mobile-first leg: all four built `/work/` pages carry the `name="viewport"` `width=device-width` meta tag
    - OFFERQ-02 WCAG-AA/fluidity leg: zero hardcoded 6-digit hex color literals and zero fixed-px container widths introduced in `themes/arcaeon/layouts/work/*.html` + `content/work/*.md`
    - Script header comment documents WHY sub-1s/3G, mobile-first, and WCAG-AA are inherited-by-reuse (no new CSS system) and therefore guarded (viewport + no-new-hex + no-fixed-px) rather than re-measured
    - `tests/validate-phase-01.sh` and `02.sh` still exit 0 (no regression)
    - Clean `hugo --minify` build exits 0
  </acceptance_criteria>
  <done>Phase 6 has a durable green regression gate proving all five ROADMAP success criteria on a clean build, with no regression to prior phases. OFFERQ-01 and OFFERQ-02 verified end-to-end: the link-out/zero-dep leg by direct assertion, and the mobile-first + WCAG-AA legs by inherited-by-reuse guards (viewport meta present; no new hex colors or fixed-px widths in the new templates/content) documented in the script header — full 3G-timing/contrast re-measurement is out of scope because no new design surface was introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site visitor → /work/ (internal) | NAV-01 adds an internal root-relative link — no new external crossing. |
| site visitor → Cal.com | Unchanged; the validation script's whole job is to PROVE the boundary stays a plain link-out (no script/widget/embed) and that CTAs carry `rel="noopener"`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | Regression re-introducing an embedded booking widget/script | medium | mitigate | `tests/validate-phase-06.sh` asserts zero booking `<script>`/widget under `public/work/`; any future regression fails the gate. |
| T-06-08 | Information Disclosure | Reverse-tabnabbing regression on link-out CTAs | low | mitigate | Script asserts `rel="noopener"` present on every `target="_blank"` anchor in built offer pages. |
| T-06-09 | Spoofing | Homepage CTA silently reverting to a wrong/mailto target | low | mitigate | Script asserts `href="/work/"` present and old mailto absent in `public/index.html`. |
</threat_model>

<verification>
```bash
# Full clean-build regression for the phase
chmod +x tests/validate-phase-06.sh
bash tests/validate-phase-06.sh     # PASS > 0, FAIL == 0, exit 0

# Prior-phase regression guard
bash tests/validate-phase-01.sh
bash tests/validate-phase-02.sh

# Spot checks
grep -q 'href="/work/"' public/index.html
grep -c 'mailto:falkensmage@falkenslabyrinth.com' public/index.html   # 0
grep -rEc '<script[^>]*cal' public/work/ 2>/dev/null | grep -v ':0' || echo "no booking scripts — good"
```
</verification>

<success_criteria>
1. Homepage secondary CTA points to `/work/` (NAV-01); old mailto gone.
2. `tests/validate-phase-06.sh` exists, follows convention, and exits 0 (FAIL == 0) on a clean build.
3. Script proves all five ROADMAP Phase 6 criteria: hub + 3 offers render; correct pitch/price/CTA; homepage → `/work/`; Cal.com URLs single-source config; clean `hugo --minify`, budgets hold (no external deps/widget, design-system reuse, `rel="noopener"`).
4. Prior-phase validation scripts still green (no regression).
</success_criteria>

<output>
Create `.planning/phases/06-offer-pages/06-03-SUMMARY.md` when done. Frontmatter must include `requirements-completed: [NAV-01, OFFERQ-01, OFFERQ-02]` and list `content/_index.md` and `tests/validate-phase-06.sh` under `key_files`. Note this plan closes Phase 6 end-to-end (all 8 REQ-IDs verified green).
</output>
