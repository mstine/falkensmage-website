---
phase: 04-production-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/favicon.svg
  - static/favicon.ico
  - scripts/png-to-ico.py
  - themes/arcaeon/layouts/_default/baseof.html
  - static/CNAME
  - .github/workflows/hugo.yml
autonomous: true
requirements: [INFRA-03, INFRA-04, INFRA-05]

must_haves:
  truths:
    - "Lemniscate favicon SVG exists with Electric Violet stroke and Neon Magenta glow filter"
    - "ICO favicon contains 16x16 and 32x32 PNG frames generated from the SVG"
    - "Browser tab shows the lemniscate favicon when site loads"
    - "GitHub Actions workflow builds Hugo Extended 0.160.1 with npm ci and deploys to Pages"
    - "CNAME file in static/ ensures falkensmage.com persists across deploys"
  artifacts:
    - path: "static/favicon.svg"
      provides: "SVG favicon with ARCAEON palette glow treatment"
      contains: "#7a2cff"
    - path: "static/favicon.ico"
      provides: "ICO favicon with 16x16 and 32x32 frames"
      min_bytes: 500
    - path: "themes/arcaeon/layouts/_default/baseof.html"
      provides: "Favicon link tags in <head>"
      contains: "image/svg+xml"
    - path: ".github/workflows/hugo.yml"
      provides: "CI/CD pipeline for Hugo to GitHub Pages"
      contains: "hugo_extended"
    - path: "static/CNAME"
      provides: "Custom domain persistence"
      contains: "falkensmage.com"
  key_links:
    - from: "themes/arcaeon/layouts/_default/baseof.html"
      to: "static/favicon.svg"
      via: "link rel=icon href=/favicon.svg"
      pattern: 'href="/favicon.svg"'
    - from: ".github/workflows/hugo.yml"
      to: "static/CNAME"
      via: "Hugo copies static/ to public/ during build"
      pattern: "hugo.*--minify"
---

<objective>
Create all production infrastructure files: lemniscate favicon (SVG + ICO), GitHub Actions workflow, and CNAME for custom domain. After this plan, every file needed for a production deploy exists locally and passes a local Hugo build.

Purpose: These are the artifacts that must exist before pushing to GitHub. Separating file creation from repo/deploy operations keeps this plan autonomous and locally verifiable.
Output: static/favicon.svg, static/favicon.ico, .github/workflows/hugo.yml, static/CNAME, updated baseof.html with favicon links
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-production-deploy/04-CONTEXT.md
@.planning/phases/04-production-deploy/04-RESEARCH.md
@hugo.toml
@themes/arcaeon/layouts/_default/baseof.html

<interfaces>
<!-- Key values from hugo.toml that favicon must reference -->
From hugo.toml [params.colors]:
  electricViolet = "#7a2cff"
  neonMagenta = "#ff3cac"

<!-- Existing baseof.html <head> structure — favicon links go after canonical URL, before </head> -->
From themes/arcaeon/layouts/_default/baseof.html:
  Line 41: <link rel="canonical" href="https://falkensmage.com">
  Line 42: </head>
  <!-- Favicon links insert between these two lines -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lemniscate favicon SVG + ICO + head links</name>
  <files>static/favicon.svg, static/favicon.ico, scripts/png-to-ico.py, themes/arcaeon/layouts/_default/baseof.html</files>
  <read_first>themes/arcaeon/layouts/_default/baseof.html, hugo.toml</read_first>
  <action>
**1. Create `static/favicon.svg`** (per D-01, D-02, D-03):

An SVG lemniscate (infinity symbol) with these exact specifications:
- viewBox="0 0 32 32", width="32", height="32"
- xmlns="http://www.w3.org/2000/svg"
- A `<defs>` block containing a `<filter id="glow">` with:
  - `<feGaussianBlur stdDeviation="1.5" result="blur"/>`
  - `<feComposite in="SourceGraphic" in2="blur" operator="over"/>`
  - Filter bounds: `x="-50%" y="-50%" width="200%" height="200%"`
- A `<path>` element for the lemniscate curve:
  - `fill="none"`
  - `stroke="#7a2cff"` (Electric Violet from hugo.toml params.colors.electricViolet)
  - `stroke-width="2.5"` (needs to be visible at 16x16)
  - `stroke-linecap="round"`
  - `filter="url(#glow)"`
- The lemniscate path should be a clean figure-eight centered at (16, 16) spanning roughly 6-26 horizontally and 10-22 vertically. Use smooth cubic bezier curves (C commands) for clean rendering at small sizes. The glow filter adds the Neon Magenta (#ff3cac) outer glow — to achieve this, add a second `<path>` element identical to the first but with `stroke="#ff3cac"` and `stroke-width="3"` and `opacity="0.6"` BEFORE the main path, so the glow sits underneath.

The SVG must be legible at 16x16 pixels — test by opening in browser and scaling down. Prefer wider, simpler curves over tight crossings.

**2. Create `scripts/png-to-ico.py`**:

```python
#!/usr/bin/env python3
"""Convert PNG files into a single ICO file. No dependencies beyond stdlib.
Usage: python3 scripts/png-to-ico.py input16.png input32.png output.ico
"""
import sys
import struct

def create_ico(png_paths, output_path):
    images = []
    for path in png_paths:
        with open(path, 'rb') as f:
            data = f.read()
        w = struct.unpack('>I', data[16:20])[0]
        h = struct.unpack('>I', data[20:24])[0]
        images.append((w, h, data))

    header = struct.pack('<HHH', 0, 1, len(images))
    offset = 6 + 16 * len(images)

    directory = b''
    for w, h, data in images:
        directory += struct.pack('<BBBBHHII',
            w if w < 256 else 0,
            h if h < 256 else 0,
            0, 0, 1, 32,
            len(data), offset)
        offset += len(data)

    with open(output_path, 'wb') as f:
        f.write(header + directory)
        for _, _, data in images:
            f.write(data)

if __name__ == '__main__':
    *inputs, output = sys.argv[1:]
    create_ico(inputs, output)
    print(f'Created {output}')
```

**3. Generate `static/favicon.ico`** by running locally:

```bash
mkdir -p static
rsvg-convert -w 16 -h 16 static/favicon.svg > /tmp/favicon-16.png
rsvg-convert -w 32 -h 32 static/favicon.svg > /tmp/favicon-32.png
python3 scripts/png-to-ico.py /tmp/favicon-16.png /tmp/favicon-32.png static/favicon.ico
```

The ICO is committed as a static asset — no CI dependency on rsvg-convert. It only changes if the favicon design changes.

**4. Add favicon link tags to `themes/arcaeon/layouts/_default/baseof.html`**:

Insert these two lines immediately after the `<link rel="canonical" ...>` line (currently line 41) and before `</head>`:

```html
  {{/* Favicon — SVG modern, ICO legacy (INFRA-05, D-01, D-03) */}}
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
```
  </action>
  <verify>
    <automated>cd /Users/falkensmage/RitualSync/falkensmage-website && test -f static/favicon.svg && grep -q '#7a2cff' static/favicon.svg && grep -q 'feGaussianBlur' static/favicon.svg && test -f static/favicon.ico && test -s static/favicon.ico && grep -q 'image/svg+xml' themes/arcaeon/layouts/_default/baseof.html && grep -q 'image/x-icon' themes/arcaeon/layouts/_default/baseof.html && test -f scripts/png-to-ico.py && hugo --minify --environment production 2>&1 | tail -3 && echo "TASK 1 PASS"</automated>
  </verify>
  <acceptance_criteria>
- `static/favicon.svg` contains `stroke="#7a2cff"` (Electric Violet)
- `static/favicon.svg` contains `#ff3cac` (Neon Magenta glow)
- `static/favicon.svg` contains `feGaussianBlur` (glow filter)
- `static/favicon.svg` contains `viewBox="0 0 32 32"`
- `static/favicon.ico` exists and is > 500 bytes (contains 16x16 + 32x32 frames)
- `scripts/png-to-ico.py` contains `def create_ico(`
- `themes/arcaeon/layouts/_default/baseof.html` contains `type="image/svg+xml" href="/favicon.svg"`
- `themes/arcaeon/layouts/_default/baseof.html` contains `type="image/x-icon" href="/favicon.ico"`
- `hugo --minify --environment production` builds without errors
- `public/favicon.svg` and `public/favicon.ico` exist in build output
  </acceptance_criteria>
  <done>Lemniscate favicon exists as SVG (Electric Violet stroke, Neon Magenta glow) and ICO (16x16 + 32x32). Both linked in baseof.html head. Hugo build succeeds with favicons in public/ output.</done>
</task>

<task type="auto">
  <name>Task 2: Create CNAME file and GitHub Actions workflow</name>
  <files>static/CNAME, .github/workflows/hugo.yml</files>
  <read_first>hugo.toml, .planning/phases/04-production-deploy/04-RESEARCH.md</read_first>
  <action>
**1. Create `static/CNAME`** (per D-08):

File contents — exactly one line, no trailing newline:
```
falkensmage.com
```

Hugo copies `static/` contents to `public/` during build, so `public/CNAME` will be in the deployed artifact.

**2. Create `.github/workflows/hugo.yml`** (per D-04, D-05, D-06):

Create the directory `.github/workflows/` first.

Exact file contents:

```yaml
# Deploy Hugo site to GitHub Pages
# Source: Official Hugo starter workflow pattern (D-06)
# Trigger: push to main only (D-04)

name: Deploy Hugo site to Pages

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.160.1
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb \
            https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb

      - name: Install Dart Sass
        run: sudo snap install dart-sass

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v6

      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"

      - name: Build with Hugo
        env:
          HUGO_CACHEDIR: ${{ runner.temp }}/hugo_cache
          HUGO_ENVIRONMENT: production
        run: |
          hugo \
            --minify \
            --baseURL "${{ steps.pages.outputs.base_url }}/"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
```

Key details:
- Hugo version `0.160.1` pinned in env var (D-05) — NOT `latest`
- Download URL uses `hugo_extended_` prefix — Extended edition required for css.Build
- `actions/configure-pages@v6`, `actions/upload-pages-artifact@v3`, `actions/deploy-pages@v5` — current official versions (per research, upgraded from D-06's v4 references)
- `npm ci` conditional — runs only when package-lock.json exists (handles font dependencies)
- `--minify` flag on Hugo build (D-05)
- `--baseURL` sourced from Pages config output — auto-resolves to custom domain once configured
- permissions block includes `pages: write` and `id-token: write` — required by deploy-pages
- `concurrency.cancel-in-progress: false` — don't cancel running deploys
  </action>
  <verify>
    <automated>cd /Users/falkensmage/RitualSync/falkensmage-website && test -f static/CNAME && grep -q 'falkensmage.com' static/CNAME && test -f .github/workflows/hugo.yml && grep -q 'hugo_extended' .github/workflows/hugo.yml && grep -q 'HUGO_VERSION: 0.160.1' .github/workflows/hugo.yml && grep -q 'configure-pages@v6' .github/workflows/hugo.yml && grep -q 'deploy-pages@v5' .github/workflows/hugo.yml && grep -q 'upload-pages-artifact@v3' .github/workflows/hugo.yml && grep -q 'npm ci' .github/workflows/hugo.yml && grep -q -- '--minify' .github/workflows/hugo.yml && grep -q 'branches.*main' .github/workflows/hugo.yml && hugo --minify --environment production 2>&1 | tail -3 && test -f public/CNAME && echo "TASK 2 PASS"</automated>
  </verify>
  <acceptance_criteria>
- `static/CNAME` contains exactly `falkensmage.com`
- `.github/workflows/hugo.yml` contains `HUGO_VERSION: 0.160.1`
- `.github/workflows/hugo.yml` contains `hugo_extended_` in the download URL
- `.github/workflows/hugo.yml` contains `actions/configure-pages@v6`
- `.github/workflows/hugo.yml` contains `actions/upload-pages-artifact@v3`
- `.github/workflows/hugo.yml` contains `actions/deploy-pages@v5`
- `.github/workflows/hugo.yml` contains `branches: ["main"]` (D-04 push-to-main only)
- `.github/workflows/hugo.yml` contains `npm ci` for font dependencies
- `.github/workflows/hugo.yml` contains `--minify` flag
- `.github/workflows/hugo.yml` contains `pages: write` and `id-token: write` permissions
- `hugo --minify --environment production` succeeds
- `public/CNAME` exists in build output containing `falkensmage.com`
  </acceptance_criteria>
  <done>CNAME file persists custom domain through deploys. GitHub Actions workflow builds Hugo Extended 0.160.1 with npm ci for fonts, minified production build, and deploys to Pages via official actions. Local Hugo build confirms CNAME appears in public/ output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local -> GitHub | Code pushed from local machine to public GitHub repo |
| GitHub Actions -> GitHub Pages | Workflow deploys build artifact to Pages hosting |
| DNS -> GitHub Pages | Domain resolution points falkensmage.com to GitHub's servers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | .github/workflows/hugo.yml | mitigate | Pin all GitHub Actions to major version tags (@v4, @v5, @v6) from official `actions/*` org only — no third-party actions (D-06). Hugo version pinned to exact 0.160.1, not `latest`. |
| T-04-02 | Information Disclosure | .github/workflows/hugo.yml | accept | No secrets in workflow YAML. GITHUB_TOKEN is auto-provisioned. Public repo — source is intentionally visible. |
| T-04-03 | Tampering | static/CNAME | mitigate | CNAME file committed to repo and included in build output. Also set in repo Settings as authoritative source. Double protection against domain reset. |
| T-04-04 | Spoofing | DNS propagation window | accept | TTL 1800s creates ~30min window during DNS cutover. Low risk — static content site with no user accounts or sensitive data. HTTPS enforced only after certificate confirmed. |
</threat_model>

<verification>
- `hugo --minify --environment production` succeeds without errors
- `public/favicon.svg` contains `#7a2cff` (Electric Violet)
- `public/favicon.ico` exists and is > 500 bytes
- `public/CNAME` contains `falkensmage.com`
- `baseof.html` has both favicon `<link>` tags
- `.github/workflows/hugo.yml` has correct action versions and Hugo Extended download
</verification>

<success_criteria>
All production infrastructure files exist locally, pass Hugo build, and are ready to push. Favicon renders the ARCAEON lemniscate glow mark. Workflow follows official Hugo starter pattern with pinned versions. CNAME ensures custom domain persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-deploy/04-01-SUMMARY.md`
</output>
